# 第十二轮修复与交付报告

2026-09-21，目标版本0.3.2。用户要求完成后直接覆盖现用程序和所有原交付便携包、安装包，桌面快捷方式继续启动现用便携目录。

## 修复

- 顶栏folder/app/group的图标占位不同，原flex压缩文字造成基线相差6.4px及下缘裁切。现统一图标方形占位、12px字号、18px完整文字行，保留原650/600ms动画与玻璃风格。测试覆盖64/88/160高度、24/40/64图标、窄屏/深色；已写入前端设计规范。
- 系统卡片“查看操作指南”原左边距只有1px，现同其他内容保持14px内缩。
- 目录底部四个文字操作共享一行；新建和复制加入同一长按手势分发，滑入松手只执行所选操作，不误开目录。窄屏横向滚动不换行。常驻命令/计数/滑选备注删除，实际移动改同行专用小图标，仍明确确认后才写磁盘。
- MOMO启动失败根因：Luma原对全部自动命令注入CARGO_NET_OFFLINE=true，ort-sys因此跳过ONNX依赖获取并输出link_error_generic/OrtGetApiBase链接错误。开发入口现继承用户环境，仅点击后执行项目既有命令；test入口仍离线，manual不变，不替用户额外运行install/fetch或cargo clean。
- 同指纹重新识别的taskId现在刷新两分钟有效期，避免刚展开就过期；清单变更/目录/客户端绑定仍重验。
- Photoshop、Illustrator、InDesign各自读取Adobe MediaBrowser MRU；校验实际软件版本资源、逐项最多256次注册表读取、字符串预检类型/字节数，排序去重并限制6–10条。打开前重新验证。其他软件暂无可靠来源时说明未接入，不伪装成真实软件历史。

## 已有证据

- npm生产构建通过；40前端单测通过；131 Edge全套通过；553 Release原生单测通过（twelfth-build/unit/e2e/native-unit.log）。
- 新增目录行布局和长按copy/create先复现2失败，修复后13目录回归通过。标签/指南先复现基线与边距故障，26矩阵回归通过。
- 原生开发环境8个真实无害子进程用例覆盖Dev/Test/Manual的cwd、继承offline未设/false/true、参数和ORT路径；新增令牌续期仍拒绝清单变更。
- Adobe读取的真实注册表计数只读检查，测试写入仅独立GUID HKCU临时夹具，未改真实Adobe记录。64项专项覆盖版本元数据、注册表预分配边界、缺失/二进制/超长/错误Unicode、令牌复核；详情r12-app-recents.md，最终实现优先于该文第一阶段32项记录。
- 实际MOMO验证：确认没有momo应用/cargo在跑后，仅清理旧失败的Luma开发启动链（旧终端输出保留），解决残留Vite占1430。沿修复后的开发环境执行同一pnpm run tauri dev命令，29.78秒完成编译，momo-canvas.exe窗口“MOMO 智能画布”出现且Responding=true；没有再出现ONNX链接错误。原项目17个Rust警告不是本次失败，不擅自修改项目代码。验证结束只清理本次新启动进程并确认端口释放。证据twelfth-momo-dev.log、twelfth-momo-running.json、twelfth-momo-cleanup.json。此项是实际项目命令验证；Luma按钮到命令的完整链路由独立原生验收另测，不混称同一次按钮操作。

## 边界

不承诺所有软件私有MRU均已接入，Adobe本地MRU也不含未写入记录的云文档；枚举样本可能遗漏更近记录。字体长名称使用单行省略和title，不以缩小不同项字号适配。新建只进入命名确认，复制不改文件，真实移动仍不覆盖、同卷限定；未增加后台轮询或装饰动画。

50–100MB闲置/150MB交互内存目标和五小时稳定性仍未通过，不能以这次功能回归替代；仍缺受信任签名和其他Windows实机。交付所需实际解压包/native/system/lifecycle、专项原生和安装升级卸载、桌面快捷方式检查及最终版本/哈希将在下文补充。

## 已完成实际覆盖交付（2026-09-21 08:08）

- 已构建并覆盖APP/native/Luma，版本0.3.2+915477f1df24e0635d0987b50606099c3ca339b5，298文件逐一与ZIP实际解压目录校验一致。
- EXE SHA-256：021A8FB94FB9C56F8119D0AA7C46D474E2FA44109804F815ADEC478F97F1978F。
- DLL SHA-256：B70AF7925B0B032E36C9FC47E789B6D966805EDD324221586A520FE97B8425DE。
- 便携包releases/luma-quick-launch-0.3.2-win-x64.zip，SHA-256 535118b1d27ed088c7dee2a3f175cdab149374784526d1ab8a6d84bceed8dd15。
- 安装包releases/luma-quick-launch-0.3.2-setup-x64.exe，SHA-256 6162ea0bcd2cba3596335cf2699a48f38aa825d3286b7c0612d12358d5420e8d。
- 原对外交付位置0.1.0/0.2.0/0.3.1文件名的便携ZIP以及0.2.0/0.3.1安装EXE全部覆盖为0.3.2内容，校验文件同步。内部历史测试副本/备份不作为现用交付入口。
- 原桌面Luma Quick Launch.lnk保持不变，目标仍为G:/Project/快捷启动项/APP/native/Luma/Luma.exe、参数--settings。已实际通过该快捷方式启动新版；唯一Luma实例PID32684仅作为本次记录，设置与浮岛响应正常，双屏热区存在。后续操作必须重查路径而非使用旧PID。
- 覆盖、重启后配置和桌面快捷方式SHA-256与退出后保存的基线一致。回滚备份APP/backups/before-0.3.2-20260921-075830。

本次实际包验证：native18/system8/lifecycle4通过；新安装EXE真实安装/升级/卸载28项通过；新增真实WebView专项8项通过（native/tests/twelfth-features-smoke.mjs）。专项验证完整12px/18px标签基线、四个文字按钮同一行且footer实测约39.5 CSS px、PNG Shell缩略图、有效令牌getPath（保留系统剪贴板）、持按滑入新建确认后才写临时目录、开发入口继承环境/cwd/去重、test优先与离线，以及真实Photoshop私有最近文件8项且完整位置显示。未点击打开PSD、未改真实Adobe历史，截图已遮蔽最近文档列表。

证据：docs/evidence/twelfth-package.log、twelfth-native-features.json、twelfth-installer-verification.log、twelfth-delivery.json、twelfth-installed-check.json、twelfth-installed-snapshot.json。构建及验收目录releases/build-cfb79636b0434369884484c734efc310。旧的候选、旧版本交付报告不能覆盖本次版本真源；已完成正常功能更新，后续只补剩余物理/长期/资源验收，不无理由重复打包替换。

## 2026-09-21 08:40 交付目录整理（替代上文历史存放路径）

用户要求删除旧打包/便携版本，最新交付全部集中 APP。已将相同0.3.2程序从 APP/native/Luma 原地迁移至 APP/Luma，298文件逐项 SHA256 一致。便携 ZIP、安装 EXE 和各自 SHA256 直接置于 APP 根下，压缩包哈希与上一交付相同，没有重新构建二进制。

已清理 releases 的所有旧交付包、build/candidate/soak 程序副本和安装验证副本，以及 APP/backups 和空 APP/native，共33个清理目标、5,312,625,646字节（约4.95GiB）。旧配置备份和历史验收日志另存 docs/evidence/retired-package-records，用户当前配置未改；源码、构建依赖和工具保留。

桌面 Luma Quick Launch.lnk 的目标、工作目录及图标均已改为 APP/Luma；保留 --settings。根目录启动Luma.cmd、构建默认输出、便携/安装包默认输出、原生验证脚本默认路径和后台跟进已同步。原自启动未开启，未额外启用。

验证：程序按完整旧exe路径正常退出后迁移；通过桌面原快捷方式实际启动新位置，唯一 Luma 实例来自 APP/Luma/Luma.exe，设置窗口响应，版本0.3.2+915477f1df24e0635d0987b50606099c3ca339b5，用户 state.json 哈希保持不变。PowerShell解析及修改的Node脚本语法检查通过。证据见 docs/evidence/app-layout-migration.json、app-layout-files.json、app-layout-cleanup-manifest.json、app-layout-after-launch.json。本次仅目录迁移，不代表新增五小时稳定性或内存预算通过。
