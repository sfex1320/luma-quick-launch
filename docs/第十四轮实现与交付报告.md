# 第十四轮：收藏、目录布局与会话记忆

## 实现

新增常态五角星：普通项目、组内软件/网址、真实目录文件与子目录、最近项目均可收藏。主栏分隔展示收藏，底色略深，复用已有玻璃图标、长按及打开行为。收藏保存快捷引用快照、手动取消，不移动或复制源文件；跨窗口继续使用既有乐观并发保存和广播。

根目录冗余标题行已删除，最深层刷新在关闭按钮左边。标准窗口4列、2整行，88×94固定格子，窄屏可滚动。末尾移动按钮现在有明确用途提示，准备了实际目录项后仍须确认移动。

收起或成功打开后保存上次项目与目录链，再次唤醒恢复。会话缓存有界（32项目×每项目32根×11子层），只存名称链，重开后逐级重新读取；失效路径截断，软件退出后清空。收藏独立持久保存。

## 稳定性修复与审查

原生旧热区计时未重新确认实际命中，且裸左键松开可触发唤醒。现在计时结束重验坐标、命中、布局、暂停和按键；单击要求热区内配对按下/松开。OLE与显式快捷键不受此鼠标驻留规则限制，650/600ms动画保留。

独立审查另发现并修复：成功打开后面板仍可保持交互并盖住目标窗口；收藏框间距未计入自动宽度；星标起点可能进入实际目录拖动链。收藏匹配使用随项目数据更新的索引，避免鼠标滑选时对全部收藏反复扫描。没有添加常驻轮询、全盘递归或后台预览播放。

## 验证记录

原生新增热区与收藏定向红→绿，完整原生单测608项通过。前端61项单测、生产构建通过。目录新增7项、星标拖动隔离、收藏类型/取消/同步/打开收起等回归通过；完整Edge最终输出、实际打包产物与覆盖结果在交付后补记。

运行前只读快照 `docs/evidence/r14-resident-before.json` 的宿主+WebView约588MiB（设置窗口当时可见、非最小化），采样间隔不连续；不能称为空闲目标达标、泄漏证明或5小时通过。DWM不可读值记null，不归因于Luma。其他Windows实机、完整物理输入/OLE/双屏及连续5小时仍需后续验收；缺受信任签名证书，本版继续按未签名测试版交付。

## 2026-09-22 最终实机验收与直接覆盖

首个实际EXE在当前Windows缩放下暴露真实问题：2030×508 CSS视口内只有首行4格完整显示，第二行被裁切。原因是目录高度另扣220px，重复预留头尾空间。改为与外层相同的40px预算，让flex按实际头尾分配；根目录与深层目录8格、340×500/360×640/640×360极矮屏等5项回归通过。没有改变650/600ms动效。

最终交付源码 `24aad2d1d747538b1673c35e356e5e473ce79c2e`，版本 **0.3.4**。前端61、原生608单测通过；完整Edge单次155通过，高度修正后追加5项回归通过。旧失败日志保留，不把首版失败覆盖成通过。

最终ZIP真实解压EXE验证：原生18、系统集成8、生命周期4、R14功能8、安装/升级/卸载28通过。R14功能覆盖四列两完整行、刷新最深层、移动箭头反馈、收藏与取消只改引用、收起后重读目录能力并恢复子层、收藏重启保留而会话记忆清空。该专项以`--startup`运行，未创建设置窗口的跨窗口同步检查明确跳过；前端同步有Edge覆盖，不能称这一项经过该专项的真实设置窗口验证。验收脚本资源统计使用哈希表导致Measure-Object失败，修正为PSCustomObject后重跑通过，未改变生产二进制。

另以真实鼠标移动覆盖冷启动后双屏各3次悬停唤出、定位、收起，通过；不是仅向热区发送消息。但未扩大为全部Alt+Tab、快捷键、OLE物理输入流程通过。

已将298个校验一致的程序文件直接替换至 `APP/Luma`，保留用户配置哈希和原桌面快捷方式；快捷方式实际启动新便携程序且仍只有一个宿主。APP根仅保留0.3.4便携ZIP、安装EXE及各自SHA256。旧0.3.3包与本轮内部构建/回滚程序副本已清理；日志与失败证据保留，不再访问已清理的旧程序路径。

- DLL SHA256：`0AF146254690B8B05DF6DBF37BE84C3D6827539EC8650FC77760E6A629FE706A`
- ZIP SHA256：`014F93BFFB5510202B69838EF276D47BCE464AA3FD0751F0C461B19B635F8675`
- 安装EXE SHA256：`3AAF0187ED34CA63AEE75A680ED2BBF8AE605DB4FC750302EC54CAD0590AD72B`

交付与检查：`docs/evidence/fourteenth-delivery.json`、`fourteenth-installed-check.json`、`fourteenth-installed-snapshot.json`、`fourteenth-physical-hover.log`。源码与测试版资产发布至本项目GitHub的新v0.3.4预发布，不修改历史Release。

## 尚未满足的稳定性目标

安装后设置与浮岛可见时，宿主+WebView共8进程，私有内存553,078,784字节（约527MiB），句柄4542。首个快照CPU无间隔值记null。专项两次工作集约576→568MiB，仅相隔数秒，不能用下降推断无泄漏。**50–150MB目标尚未达标**，也没有连续18000秒最小化设置+收起浮岛记录；本次不宣称5小时稳定、CPU目标通过或系统卡顿已根治。

后续现用APP运行时只读，不为补测停止用户程序。全体Luma退出且用户不操作其他前台、非全屏时，才运行唯一独立配置的剩余物理交互与冻结长测。保护触发/退出/休眠/采样断档均不能通过；DWM未知值记null，不能把整机DWM全部归因Luma。仍缺受信任签名和其他Windows实机验证，本版保持测试版定位。
