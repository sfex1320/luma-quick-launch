# 第十三轮：抓手滚动与快捷键

## 实现

- 主工具条保留右键水平拖动；目录、集合、最近文件子菜单改为左键空白抓手，7px 后开始滚动，按钮长按与引用拖拽不受空白抓手接管。
- 多入口软件组只显示全宽“进入”；单软件保留“打开软件 / 最近”。取消图标角落的展开箭头，长按、悬停进入和右键入口保留。
- 设置与备份新增快捷键卡片，可录制 Ctrl / Shift / Alt 任意组合和单键，绑定主面板、搜索、设置、整理、项目或成员。当前目录的打开、新建、复制、运行项目使用已有目录令牌操作。
- 用户已确认单键仅面板有焦点时生效；组合键可全局。输入框、录制、IME 和对话框不触发面板快捷键。全局注册冲突显示实际状态，录制期间暂时释放，结束/失焦/退出恢复或清理。
- 原生按配置 ID 执行，配置删除、改绑、排队或窗口查找期间失效时停止；重复按键不重复启动，沿用窗口复用服务。项目展开等动作等待真实浮岛恢复、布局确认后发送。

## 已完成验证

- 前端 59 项单测通过，生产构建通过。
- 完整 Edge E2E 143 项通过，含空白抓手、成组入口、焦点范围、录制与注册状态恢复。
- 原生 589 项单测通过，含注册释放、旧消息、激活排队、启动授权在后台等待期间变更。
- 独立前端与原生审查发现的问题已修复并复审。
- 上述结果来自本轮输出 `docs/evidence/thirteenth-*.log`，不等于真实全局按键或长期稳定性验收。真实 Windows、安装、覆盖结果在下文续记。

## 交付状态与边界

已直接覆盖 0.3.3：便携程序 APP/Luma；APP 根目录仅保留 0.3.3 ZIP、安装 EXE 和各自 SHA256。旧 0.3.2 包、内部构建与旧程序回滚副本已清理，测试日志留 docs/evidence。桌面快捷方式仍指向 APP/Luma/Luma.exe，用户 state.json 哈希保持不变。当前以 --startup 静默启动新版，避免打断用户正在编辑的 Illustrator；本轮未通过桌面链接再次打开设置窗口。

交付源码为 `7eacc3a7eabb7281724aad9e2d593a8c82b8d93c`；其后仅测试夹具和文档调整，不改变交付二进制。298 文件与 ZIP 实际解压内容逐一哈希一致。DLL：`BB25146ED8E6E0DB0A47F07359DD9A48D079FB10286FD5BB8086B49304E41911`。实际解压程序 native 18 / system 8 / lifecycle 4 检查通过，安装、升级、卸载 28 项通过。证据：`thirteenth-delivery.json`、`thirteenth-installed-check.json`、`thirteenth-installed-snapshot.json`、`thirteenth-installer-result.json`、`thirteenth-package-verification/`。

### 真实按键：部分验证，完整流程尚未通过

使用唯一解压 EXE、独立配置、专用临时 WinForms 软件，未启动 Photoshop/MOMO 或改动用户项目。已观测实际 Windows 注册冲突、解除冲突后重新注册、Ctrl+Shift+Alt+G 启动一次并复用窗口、外部文字/IME 和面板专用 F8 不启动软件。retry9 通过从真实 WebView Suspend 恢复，连续两轮全局项目键分别只发送一次递增 serial 的展开事件。panel 专项日志确认真实点击取得 HWND 与 document 焦点，扫描码 F9 连续按下仅第一次非 repeat，编辑态只切换一次、原生激活一次；下一次按键前前台已切回用户 Illustrator，保护停止。

早期夹具存在隐藏 WinForms、PowerShell 5.1 ushort 别名、物理输入缺少扫描码及焦点检查间隔问题，已修正；输入使用 [Microsoft KEYBDINPUT 扫描码](https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-keybdinput)，不改变用户输入语言。retry6 曾观察虚拟键注入时一次按住产生两次展开，retry9 没有复现，尚不能据此排除全部重复输入问题。所有失败日志保留，不能把重跑部分通过汇总成全流程通过。最终夹具增加“前台属于其他应用立即停止、不重新聚焦”的保护；此保护仅静态检查，本轮没有再次操作正在使用的 APP。

剩余：稳定独占时段内完整真实按键流程（第二次单键、单键启动、录制暂停/恢复、改绑后物理激活与退出释放）、所有物理抓手/级联与双屏回归。`LUMA_SHORTCUT_SMOKE_SCOPE=panel` 只验证面板/录制子集，结果必须标注 scope，不得冒充 full。测试仅能在现用 APP 已退出且非全屏、无其他应用正在被用户操作时进行；不得为例行监测关闭现用 APP。

单键需要先点击 Luma 面板取得焦点；悬停呼出不会抢其他程序焦点。全局组合键受 Windows 和其他应用占用影响，以设置显示的注册状态为准。仍为未签名测试版；5 小时连续挂机、其他 Windows 实机与 50–150MB 内存目标未通过，不能把本轮交互验收当作这些项目已完成。
