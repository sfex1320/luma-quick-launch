# 第十轮：手动启动、文件缩略图与级联目录

更新：2026-09-21。代码与独立候选已完成；本机原生功能测试通过，稳定性与最终安装交付仍未验收完成，用户APP目录尚未替换。

## 使用方式

1. 在项目/快捷项的编辑窗口，为文件夹启用“手动启动命令”，填写工作目录与CMD命令，保存后该根目录优先使用手动配置。点击“手动启动”才执行；可用“恢复自动识别”撤销覆盖。支持与绑定目录不同的工作目录。CMD无法将UNC共享直接作为cwd时明确提示使用映射驱动器。
2. 子目录的文件图标显示静态内容缩略图，不新增预览窗口或视频播放。通过Windows Shell读取图片、视频及设计文件的系统缩略图；PSD/AI/CDR是否可显示取决于已安装的Shell处理器，不支持时稳定回退。已实测PNG，尚未实测所有视频、PSD/CDR/AI格式。
3. 快速单击直接打开；按住300ms展开菜单，继续按住移动到文件夹上停留450ms，右侧展开下一层，父层与成组入口保留。松手打开当前目标，移出取消。屏幕不足时横向浏览，最多同时保留12层，格子88×94。
4. 文件夹悬停250ms出现两个等宽大区域：“打开”和“进入”。进入只浏览；快速点击不等分割仍直接打开。键盘ArrowRight也可进入。

展开导致布局变化时，必须继续移动鼠标才允许松手打开，避免列移位把另一个图标带到鼠标下误启动。收放保持650/600ms滑移，没有增加后台装饰动画。

## 实现边界与审查

- 状态schema1新增可选launch；protocol1沿用detectTest/runTest保存ID及短期令牌。保存/识别不执行。命令或cwd修改使旧任务失效；同工作目录终端打开/启动中返回BUSY。
- 原生缩略图固定2个STA工作线程；排队、待处理、缓存及字符数有上限，过期队列不迟到提取，COM/GDI释放。前端可见图标懒加载、2并发32排队、128缓存项，离屏移除图像，无全盘扫描或重试循环。
- 交叉审查修复了命令trim会改变用户字面值、工作目录控制字符漏检，以及合法文件名中的shell关键词被误认为命令的问题。级联列仅在可视面板内命中，不把屏幕外列加入原生区域；成组父菜单不卸载，持按捕获持续有效。
- 本地命令属于用户显式授权执行，非代码沙箱；自动识别的“不恢复依赖”限制保持。没有通过接口接收任意临时命令/路径。

## 最新验证

|验证|结果与范围|
|---|---|
|前端单元|33通过，包括命令字面值、缓存并发/溢出/刷新|
|完整Edge E2E|78通过，包括单击、分割、连续三级、成组、布局移位、移出取消、窄屏及原收放|
|原生Release单元|353通过，包括真实隐藏CMD命令样例、令牌失效、并发及缩略图边界|
|标准native-smoke|18检查通过，真实WPF/WebView2、双屏热区消息各3次、Shell打开/复用、设置/搜索与真实文件对象拖入|
|第十轮专项native|7检查通过：仓库拖入、真实Shell 96×96 PNG缩略图、不支持文件回退、自动Node一次执行及cwd、终端去重、手动CMD独立cwd、实际目录令牌连续三级/移出取消、恢复自动后无测试入口隐藏按钮|

标准与专项测试均运行同一独立候选，使用各自临时配置。浏览器通过CDP连接实际宿主，未注入模拟桥接；拖入使用WebView文件对象，这不替代所有实际资源管理器OLE拖动的物理验收。终端实际写入了测试标记；仍需补核可见终端输出、物理Alt+Tab、收回中最小化等边界。PowerShell转存子进程stderr时出现CLIXML格式提示；以各隔离目录result.json的passed和实际输出文件核对，未将该提示当作应用异常证据。

本机证据（不上传用户配置/日志）：

- `docs/evidence/tenth-frontend-e2e.log`、`tenth-build.log`、`tenth-native-smoke.log`、`tenth-features-native.log`。
- 标准原生结果：`C:/WINDOWS/TEMP/luma-native-smoke-f1f244cf-567f-4319-9dd9-8ffe57d6f7ed/result.json`。
- 专项原生结果及截图：`C:/WINDOWS/TEMP/luma-tenth-native-840c847a-be41-4ac1-b463-ba82b06e6ddc/result.json`、`cascade.png`。

候选：`releases/candidate-0.3.0-r10/Luma.exe`，自包含win-x64，约160.4MB。

- Luma.dll SHA256：`88E7AF1C45D31AED83642EBCA5DDBA919DBAB05DAF97367BF8FF0F37139E6450`。
- dist/index.html SHA256：`313C8E1EAB8CA5226EA45C5C0D2717F5E5576B5F5DDC402F3770A576DD4FA6B1`。

## 长测异常与后续交付门槛

第八轮冻结副本在02:35被watchdog停止，原因sustained-dwm-cpu-limit，6503.4秒/208样本，未满足18000秒。私有内存初始349.9MB、峰值643.6MB、最终449.4MB；句柄4028→4458。最后约18分钟内存448.8～450MB，句柄4477→4458；最后3个样本DWM单核百分比62.105/130.632/128.372，Luma树CPU为0.200/0/0。DWM的100%是一个核心，不是整机100%。无采样大断档（最大36秒），用户配置哈希保持。

日志显示02:14:46浮岛暂停，02:28只有未达唤出时长的热区经过，随后DWM高占用。期间确有实际交互及打开文件，不能作为连续纯挂机。内存先增长再回落不证明无泄漏；低Luma CPU也不足以排除GPU/桌面合成影响。DWM基线为null、没有GPU显存与完整逐进程时间序列，不能归因于Luma或声称已排除Luma。分析见本机`docs/evidence/eighth-soak-final-analysis.json`。

仍需继续，不能跳过后发布：

1. 为下一轮长测保存逐进程角色/内存/句柄/CPU、系统可用内存、每次响应性和最小化/收起状态及用户交互区间；补采不运行Luma时DWM基线。保持保护阈值，不为通过而抬高，任何休眠/保护/中断均不算通过。
2. 第十轮唯一候选、独立配置，真实双屏收回途中切换/最小化/Alt+Tab、物理鼠标返回、持续持按/实际OLE拖入；前台全屏时等待，不能关闭游戏或绕过保护。
3. 手动和自动命令的可见终端输出、旧清单/配置令牌拒绝、普通目录无入口及更多Shell缩略图格式验证。新候选最小化暂停恢复及连续5小时场景，不复用旧sourceCommit结果。
4. 最终候选执行system/lifecycle、安装/升级/卸载；必要验收通过才更新APP、便携ZIP与安装EXE、已有GitHub测试版Release。仍缺受信任签名证书和其他Windows实机，不能称正式稳定版。
