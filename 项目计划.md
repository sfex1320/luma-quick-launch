# 顶边项目启动器｜项目计划

版本：v0.4 产品背景与迭代计划 · 2026-09-19

当前已交付前端 v0.1，原生内核由用户指定的 Z Code / GLM 5.3 接入。当前权威实现与接口见 AGENTS.md、src/contracts.ts、docs/前端设计规范.md、docs/内核接口协议.md；本文保留产品方向与长期计划。以下未进入前端 v0.1 的功能仍是后续目标，不代表已实现。

## 1. 产品目标

让用户在当前工作窗口中，通过屏幕顶边快速打开软件、文件夹及项目资料，并逐步支持按项目找回已打开的窗口。

采用“顶边快捷栏 + 项目堆叠”的结构：每个项目显示为一个水晶文件夹堆叠，长按展开该项目的其他文件夹；项目详情面板承载更多固定资料与工具。优先保证启动迅捷、磨砂毛玻璃与水晶图标质感、较低常驻资源占用。

本次新增的已确认方向：项目内文件夹堆叠、长按展开后滑向目标并松手打开、磨砂毛玻璃、水晶图标、低占用。下文“单击主目录”、展开布局及具体参数属于建议默认值。

本轮补充：面板只占顶边局部区域，圆角与明显磨砂质感；宽度、高度、图标大小独立可调；图标和字体自适应。用户希望减少操作，因此默认值、查证、文档维护及后续常规调试由开发方完成，只有无法从上下文决定且会改变产品方向的问题才提出。

初步平台假设：Windows 11 桌面、鼠标和键盘、本地优先、中文界面。Windows 10 兼容需求和具体最低系统版本在开发前确定。

成功标准：常用软件唤出后一次点击即可打开；项目资料最多两次点击即可打开；普通使用中不频繁误触、不抢输入焦点、不会丢失配置。

## 2. 已确认需求与建议补充

| 类型 | 内容 | 处理方式 |
|---|---|---|
| 已确认 | 屏幕顶边停靠、缩进动效 | 首版实现 |
| 已确认 | 鼠标悬停显示，点击启动软件或文件夹 | 首版实现 |
| 已确认 | 按项目展示，快速打开项目 | 首版实现项目入口和固定资料 |
| 已确认 | 同一项目的文件夹堆叠存放 | 首版以项目堆叠作为主要入口 |
| 已确认 | 长按展开堆叠，滑向目标文件夹后松手打开 | 首版核心手势；提供展开按钮与键盘等价操作 |
| 已确认 | 磨砂毛玻璃与水晶图标 | 首版核心视觉，提供低功耗和不透明回退 |
| 已确认 | 占用率不用很高 | 增加隐藏、展开和连续操作的 CPU、GPU、内存测量 |
| 已确认 | 局部宽度、圆角、强磨砂 | 顶边中央浮岛式面板，不铺满屏幕 |
| 已确认 | 宽高和图标尺寸可调，文字图标自适应 | 首版提供实时预览、合法范围和溢出处理 |
| 已确认痛点 | 资源管理器窗口混在任务栏，难以区分 | 单独开展窗口识别验证，不能把图标分组当成此问题已经解决 |
| 建议 | 文件入口、拖入添加、排序、重命名 | 首版实现 |
| 建议 | 快捷键唤出、搜索已添加的入口 | 首版实现 |
| 建议 | 防误触、全屏避让、多屏和缩放适配 | 首版实现并实机验证 |
| 建议 | 路径修复、配置备份、托盘管理 | 首版实现 |
| 建议 | 一键打开项目工作组合 | 后续版本 |

## 3. 产品结构

### 3.1 顶边快捷栏

- 默认停靠在选定屏幕顶边中央，仅占局部宽度；默认使用用户指定宽高，另提供可选的“随内容收缩”，新增入口不自动撑满屏幕。
- 分为常用软件、置顶项目、搜索和管理入口；数量过多时进入“更多”，不无限挤小图标。
- 软件保留可辨识的原生标识并配统一底座；项目显示“水晶文件夹堆叠 + 短名称 + 数量”，支持辅助色。
- 点击软件、独立文件夹、文件，执行对应打开动作。项目堆叠建议单击打开主目录，长按展开同项目文件夹；独立展开按钮也能直接打开堆叠。
- 右键提供编辑、从快捷栏移除、打开所在位置；移除入口不会删除实际文件。
- 管理模式支持排序；提供菜单或键盘方式，避免只能靠拖拽。

### 3.2 项目堆叠与详情面板

项目是资料入口的集合，可以关联多个磁盘目录，不要求实际文件都搬到一个位置。

绑定主目录后创建项目堆叠；用户选择其常用子目录加入，也可添加磁盘其他位置的目录。首版不依据文件夹同名自动判定项目归属，不递归扫描整棵目录树。导入子目录候选时只按需浅层读取，由用户决定加入项。

收起时最多绘制 3 层错位文件夹轮廓，实际数量用徽标表示，避免绑定越多绘制越重。主目录始终是正面的主图标，堆叠计数包括主目录；长按展开的快捷选择区主要展示其他已绑定目录，并提供“打开主目录”和“项目详情”入口。

展开方式比较：

| 方案 | 优势 | 取舍 |
|---|---|---|
| 向下阶梯展开，超出数量改网格（推荐） | 顶边下方直接滑选，名称横排清楚，仍有堆叠层次 | 两种布局需采用固定阈值并保持各自排序 |
| 扇形展开 | 少量目录时视觉鲜明 | 长名称、靠屏幕边缘、较多目录时易拥挤 |
| 始终使用网格面板 | 容量大，定位稳定 | 堆叠展开的空间连续感较弱 |

推荐默认：其他目录 1–6 个时向下阶梯展开，文字不旋转、点击区互不重叠；超过 6 个时在手势开始前确定为网格布局，带名称与搜索。一次手势中布局不切换。零个其他目录时显示添加入口，不执行空的批量启动。

目录顺序由用户固定，不因访问频率自动重排；最近使用区域独立呈现，避免肌肉记忆失效。

示例项目“品牌官网”：

| 分区 | 示例入口 |
|---|---|
| 主目录 | G:\\Projects\\品牌官网 |
| 常用文件夹 | 设计素材、源代码、客户资料、导出文件 |
| 固定文件 | 需求文档、设计稿、交付清单 |
| 项目工具 | 编辑器打开该项目、设计软件 |
| 最近访问 | 仅记录通过本工具打开的项目入口 |
| 已打开窗口（后续） | 能可靠关联到此项目的窗口 |

首版以手动固定资料为主，明确区分“固定资料”和“实时目录内容”。目录浅层浏览可放到后续版本；全盘检索、文件搬移和完整文件管理器不进入首版。

界面布局示意：

```text
屏幕顶边
                  ── 唤出提示 ──
悬停展开
    [常用软件 …]  │  [品牌官网] [视频项目]  │  [搜索] [管理]
                  单击主图标 → 打开主目录
                  长按主图标 → 展开其他文件夹
                         ↓ 项目详情
                  品牌官网     [打开主目录]
                  常用文件夹   素材 / 源码 / 交付
                  固定文件     需求文档 / 设计稿
                  项目工具     编辑器 / 设计软件
                  最近访问     本工具内的访问记录
```

### 3.3 管理与设置

独立设置窗口管理项目、入口、显示屏、唤出区域、外观尺寸、主题、动效、快捷键、备份和开机启动。外观页优先展示宽度、高度、图标大小三个滑块与数值输入，调整时实时预览，提供撤销本次调整和恢复默认。托盘菜单提供显示、暂停边缘唤出、设置及退出。

首启用简短引导帮助用户创建第一个项目，也允许跳过。开机启动为显式设置项。

## 4. 唤出与收起规则

下列数值为原型起点，不是已验证的最佳值。尺寸使用逻辑像素，并随系统缩放换算。

| 状态或动作 | 建议行为 |
|---|---|
| 隐藏 | 顶边中央保留约 120–200 × 3–4 的提示及命中区域，宽度可调整 |
| 鼠标进入热区 | 停留约 180 ms 后展开，经过时尽量不触发 |
| 展开 | 从顶边向下滑入，约 200 ms；尽量保持位置与视觉连续 |
| 离开 | 离开快捷栏、项目面板及关联弹层的联合区域约 400 ms 后收起 |
| 收起 | 向上缩回，约 150 ms |
| 快速返回 | 取消收起计时；正在收起时可平滑反向展开 |
| 操作菜单、拖拽、编辑 | 暂停自动收起，避免操作中消失 |
| 鼠标唤出 | 展示时不自动夺取当前应用的键盘输入焦点 |
| 快捷键唤出 | 接受键盘输入，方向键导航，Enter 打开，Esc 分层关闭 |
| 点击启动成功 | 默认收起；启动失败则保留入口并显示原因 |
| 项目短按 | 建议在松手时打开主目录，未松手不启动 |
| 项目长按 | 建议约 300 ms 后展开堆叠，并取消本次短按启动；阈值可配置 |
| 全屏应用 | 默认抑制边缘自动唤出，允许用户通过快捷键主动调用 |
| 减少动态效果 | 支持关闭位移动效，保留即时状态反馈 |

特别验证浏览器标签页、最大化窗口标题栏、窗口拖到顶边时的系统贴靠交互。支持调整热区位置与宽度、暂停边缘唤出，以处理冲突。

首版支持“选择一块显示屏”，拔掉目标显示器后回退到主屏；后续再做多屏同时显示。必须测试上下排列显示器时鼠标能否顺畅跨屏。

### 4.1 已确认的长按滑选交互

已确认主路径：长按展开后继续按住、滑到目标并松手打开。建议兼容在原图标位置松手后保留面板、再点击目标的方式，方便不习惯连续手势的用户。长按本身不触发批量打开。

1. 按下立即给出轻微下压反馈，启动一次性长按计时。
2. 长按前移出图标或移动超过约 6 个逻辑像素，取消短按和长按候选，不立即启动目标。排序仅在管理模式进行。
3. 达到阈值后展示堆叠，取消短按；使用指针捕获保证跨过图标到浮层期间状态连续。
4. 以固定的最终布局计算命中区。展开动画尚未完成时允许快速选择，但必须已进入目标命中区并显示选中反馈，避免图标动画经过鼠标时意外启动。
5. 松手在有效目标上只启动该文件夹一次；松手在主图标上只保留展开状态；松手在浮层外取消；空白处不启动。
6. Esc、失去指针捕获、屏幕配置改变或系统中断时取消手势，不执行残余点击。
7. 菜单打开、长按滑选时不自动收起；Esc 先关闭堆叠，再收起快捷栏。
8. 展开箭头、右键“展开堆叠”与键盘展开操作提供等价入口。首次使用显示简短提示，长按不成为隐藏的唯一通路。

“全部打开”只作为清楚标记的独立操作，后续版本实现。文件夹较多时显示数量和启动进度，不绑定为默认长按行为。

## 5. 视觉方向

采用磨砂毛玻璃浮层和水晶文件夹图标作为首版核心视觉。毛玻璃用于快捷栏及展开容器，水晶质感用于图标；以材质、高光、轮廓和层次形成质感，同时约束重绘与特效成本。

- 玻璃底板：带中性色遮罩的背景模糊、细亮边、少量静态噪点；真实背景杂乱时仍确保名称可读。
- 水晶图标：半透明彩色外壳、清晰倒角、顶部高光和底部柔和投影，以预制资源或缓存矢量呈现。
- 色彩：冰蓝、浅紫、青绿等辅助色区分项目，名称保持清晰；避免所有图标同时高饱和发光。
- 动效：堆叠层从同一锚点向下展开，约 160–220 ms；同时或极短错峰完成，避免逐项等待。长按计时和动效都不能拦住已有效命中的启动操作。
- 不采用常驻实时 3D 折射、连续流光、背景截图逐帧模糊或每个图标各自叠加一层背景模糊。
- 标准模式展示真实毛玻璃；节能模式减小材质成本，必要时使用保持相同层次的实色底板。尊重系统透明度和减少动画设置。

- 快捷栏默认 640 × 88 个逻辑像素，水晶图标默认 40，点击区域至少约 44 × 44；尺寸可调范围与自适应规则见下节。
- 项目面板建议宽度约 360–460，长列表限制高度并滚动。
- 使用系统中文字体；正文建议 14，辅助文字不小于 12，尊重系统文字缩放。
- 背景必须保证文字可读；透明效果关闭后仍保留完整层次。
- 统一悬停、按下、键盘焦点、启动中、不可用状态；错误不能只靠颜色表达。
- 项目以名称为主要识别线索，颜色只是辅助。
- 图标悬停以高亮和轻微缩放为主，避免推动相邻图标或长时间弹跳。

参考设计 token：背景 #111827、浮层 #1F2937、主文字 #F9FAFB、次文字 #D1D5DB、强调色 #A5B4FC。浅色主题另配对应 token；实现后对真实背景、透明效果和全部状态测量对比度，不能只检查色值。

参考 ui-ux-pro-max 的动效、键盘可达性、清晰状态和性能原则；搜索结果里的网页营销布局、远程字体及通用配色不直接套用到桌面快捷栏。

### 5.1 局部浮岛与可调参数

“不需要做全局”在此按上下文落实为不覆盖整个屏幕宽度、不接管整个桌面。已有的可选全局唤出快捷键保留，用户可关闭。

下表是设计默认值，不是已实现功能。尺寸均为 DIP（逻辑像素），由应用统一转换为当前显示屏物理坐标。

| 参数 | 默认 | 调整方式与边界 |
|---|---|---|
| 面板宽度 | 640 | 320 至 min(1120, 当前可用屏宽的 90%)；屏幕过小时以可用宽度为准 |
| 面板高度 | 88 | 通常 64–160；系统文字放大时按内容提高可读最小高度 |
| 图标尺寸 | 40 | 偏好值 24–64；实际尺寸受面板高度约束，保持纵横比 |
| 圆角 | 22 | 12–32，实际圆角不超过短边的一半 |
| 文字 | 自动 | 常规字号随实际图标尺寸在 12–18 间变化，再应用系统文字缩放 |
| 面板位置 | 顶边居中，视觉下沿与圆角完整 | 可调水平偏移；目标显示屏改变后重新夹取到屏内 |
| 顶边留白 | 8 | 0–16；热区仍在屏幕顶边，热区到面板的走廊连续可达 |
| 磨砂 | 强 | 标准模式默认强磨砂，提供柔和、强及节能预设 |

宽度指主快捷栏宽度；展开堆叠按内容独立计算，但必须限制在当前显示屏可用区域内，不能因为主面板很窄就把展开目录挤成一列难读文字。

强磨砂通过背景材质、中性色遮罩、轻微噪点及边缘高光共同形成。若平台只支持材质预设，不向用户展示无法真实控制的“模糊半径”数值。调尺寸不逐帧重建毛玻璃资源；隐藏区域不保留大面积透明输入拦截层。

### 5.2 自适应与参数冲突规则

原则：保留用户偏好值，同时计算当前屏幕上的有效值；空间不足时在设置页说明实际使用值。换回大屏后恢复偏好，不永久覆盖原配置。

1. **高度与图标。** 从用户图标偏好尺寸向下尝试，直至“上下各 8 的留白 + 图标 + 4 的图文间距 + 一行文本 + 6 的堆叠上沿空间”能放入面板。图标最低 24。仍放不下时提高合法最小高度，不裁掉文本或点击区域。
2. **字号。** 建议基础字号为 clamp(round(实际图标尺寸 × 0.25 + 4), 12, 18)，再应用系统文字缩放。实际布局使用字体测量结果，不假设所有中文、英文、emoji 都等宽。
3. **可见容量。** 单元格宽至少 max(64, 图标尺寸 + 24)，间隔 8，左右内边距各 16；预留约 96 给搜索、管理等动作。容量不足时保留一个“更多”入口，把超出项目移入弹层，顺序保持不变。
4. **长名称。** 主栏采用一行省略，悬停和键盘焦点均可查看完整名称；展开列表允许两行，并提供完整路径提示。同名目录显示父目录辅助信息。
5. **窄屏与文字放大。** 优先将次要动作归并到管理菜单，再减少可见入口；极端窄屏下保留可用的项目列表入口。禁止通过整体缩放把文字压到下限以下。
6. **手势稳定。** 长按滑选期间不接受实时重排；设置调整暂缓到手势结束。屏幕拔插或 DPI 改变必须先取消当前手势，再重排，避免命中区跳动后误开目录。
7. **预览与保存。** 滑块拖动只更新可视布局，停止调整后约 400 ms 合并写入一次；取消恢复进入本次调整前的配置。恢复默认可撤销，移除项目与外观重置分开。

以默认 640 宽、40 图标为例，上述容量规则通常能放约 7 个入口单元；有溢出时其中一个用于“更多”。这是布局计算示例，最终按文字缩放与实际控件测量确定。

### 5.3 少操作的配置流程

- 将文件夹拖到添加区域即可创建同名项目，自动读取图标，主目录自动绑定。
- 将其他文件夹拖到已有项目上，显示“加入此项目”反馈后松手完成；此操作添加入口，不移动真实文件。
- 只有拖动过程中明确进入目标项目才出现合并反馈；不因简单经过就变更归属。
- 提供紧凑、标准、舒展三档尺寸预设，并保留三个独立滑块，默认标准档即可使用。
- 显示屏消失时自动回退主屏；路径失效保留原入口并提供重新定位；配置损坏优先恢复上次有效版本。
- 后续开发交付尽量提供可直接启动的程序，由开发方完成构建、测试和常规故障排查；用户不需要手工运行开发命令。

## 6. 范围与优先级

### P0：可日用的首版

1. 单实例、托盘管理、顶边停靠、延迟唤出及收起、可关闭动画。
2. 添加软件、快捷方式、文件夹、文件；文件选择器和拖入添加；编辑、移除、排序。
3. 项目创建、编辑、归档；主目录、多目录及固定文件；水晶堆叠、长按展开、快捷选择；项目独立工具入口。
4. 搜索已添加的入口及项目，支持名称和别名；全局快捷键可配置并检测占用。
5. 强磨砂与水晶图标、局部圆角浮岛、宽高与图标大小调整、文字图标自适应、深浅主题、节能材质回退、屏幕选择、基础 DPI 适配、全屏避让、键盘导航。
6. 路径失效提示与重新定位；启动失败反馈；不因网络盘不可达而卡住界面。
7. 本地配置、原子保存、自动备份、导入导出、版本迁移和损坏恢复。

### P1：项目窗口与工作组合

1. 在项目面板展示能够可靠识别的已打开窗口，点击恢复或切换。
2. 优先切换到可确认已打开的目标；提供“重新打开”操作作为可见后备。
3. 一键打开项目的选定文件夹、工具和文档，报告每项结果，不自动关闭其他项目窗口。
4. 项目目录浅层浏览、拼音和首字母搜索、多个屏幕的唤出策略。

窗口识别虽安排在 P1 交付，但需在第一阶段提前做技术验证。如果“找回已打开的项目窗口”是首发硬性要求，则将其提升到 P0，并相应缩减装饰性和批量启动功能。

### P2：使用反馈驱动的增强

按需考虑网址入口、更多停靠方向、工作区布局恢复、项目模板和可选同步。不在初版增加账号、插件市场或 AI 分类，以控制常驻程序的复杂度。

## 7. 资源管理器窗口问题的边界

项目入口分组能解决“去哪里打开”，窗口关联解决“已经打开的在哪里”。首版完成前者，并不代表后者已经解决。

Windows 任务栏使用应用标识关联进程、文件和窗口，这套机制不等同于按用户项目目录分组。微软文档描述了应用管理自身标识及窗口分组的方式；不能由此推断可以稳定重组任意第三方应用或资源管理器窗口。

建议由本工具提供独立的项目窗口列表，先验证如下能力：

- 能否获取当前系统版本中资源管理器窗口的可靠目录信息。
- 多标签页窗口能否取得所需标签信息，以及是否能准确激活目标标签。
- 最小化、不同虚拟桌面、权限差异和外接显示器变化时的切换行为。
- 从外部打开、同名文件夹、父子项目目录重叠时的归属方式。

路径匹配必须按完整路径段判断，优先最具体的项目根目录；用户显式指定的归属优先。无法可靠识别的窗口放入“未归属”，不得只靠标题猜测后标成准确匹配。

对多标签页不具备可靠支持时，应明确展示实际支持范围，例如只切换窗口、不能定位非当前标签。界面不能声称已定位目标标签。

## 8. 技术方案建议

当前选型已调整为 React + TypeScript 前端、C# / .NET 10 + WPF + WebView2 宿主。前端已实现，不再使用纯 WPF 重画业务界面；Windows 集成层处理热键、显示屏、Shell 启动、托盘及窗口交互。完整边界见内核任务书。这是根据用户将前后端分工后的架构决定，不是跨框架性能实测结论。

开源调研结论：独立实现本项目的小面板、项目模型和手势状态机；参考 Cairo 的目录堆叠、Seelen UI 的 Dock 设置与主题、Flow Launcher 的缓存方式。ManagedShell 只在确实需要其能力时评估，首版不直接引入整套桌面外壳。详见《开源项目调研.md》，其中记录查阅范围、许可证标识、版本快照与源码链接；这些项目均未在本机安装或运行，不能据此作性能排名。

在首轮原型中验证无边框窗口、不激活显示、置顶、透明命中区域及动画组合。纯网页外观演示不能替代这些桌面行为验证。若材质或窗口限制不能满足要求，再评估 WinUI 3。

UI/前端现已使用 Web 技术构建。必须测量宿主和全部 WebView2 子进程的占用，原计划资源目标继续作为待验证目标，不据网页截图宣称达标。

材质选择：优先验证系统提供的背景 Acrylic 是否能用于本项目浮层；微软将 Acrylic 用于临时浮层，而 Mica 属于不同的背景材质，不能把它当作实时透出背后窗口的同义词。具体 WPF 互操作与系统版本支持必须实机确认，不能仅据材质指南认为开关即可接入。

低占用策略：

- 收起后暂停全部装饰动画和内容刷新，只保留必要唤出事件与低成本检测；不用常驻 60 FPS 循环检查鼠标。
- 首版只保留快捷栏和当前项目的必要可视内容，其他堆叠按需创建；长列表采用虚拟化。
- 图标按尺寸缓存，设置缓存预算并释放淘汰项；数量徽标变化不重建整套图标。
- 展开期间只动画位移、缩放和透明度，不逐帧改变模糊半径；不因动画反复触发布局计算。
- 毛玻璃限定在可见浮层范围；设置界面关闭后释放相关可视资源。
- 不后台递归扫描项目目录；网络位置异步加载且可取消。
- 对比标准与节能模式，测量空闲、展开静止、连续唤出、后台播放视频四类场景。GPU 同时观察应用和桌面合成器，避免把合成成本漏算成“零占用”。

建议模块：

| 模块 | 职责 |
|---|---|
| Launcher.UI | 快捷栏、项目面板、设置、主题和键盘操作 |
| Launcher.Core | 项目与入口模型、搜索、顺序、显隐状态机 |
| Launcher.Windows | Shell 启动、图标提取、热键、显示器和窗口查询 |
| Launcher.Storage | 配置读写、迁移、备份、恢复 |

首版使用带 schemaVersion 的 JSON 配置，图标缓存独立存储。建议按用户保存在本地应用数据目录；通过临时文件与替换机制保存，保留上次有效备份。

数据实体：

- Project：id、名称、辅助色、图标、主目录、关联根目录、顺序、归档状态。
- LaunchItem：id、可选 projectId、类型、名称、目标、参数、工作目录、图标、别名、顺序。
- Preferences：显示屏、热区、延迟、长按阈值、主题、材质档位、动画、快捷键、全屏策略、开机启动；panelWidthDip、panelHeightDip、preferredIconSizeDip、cornerRadiusDip、horizontalOffsetDip、topGapDip、widthMode、fontScaleMode。有效尺寸只作为运行期布局结果，持久化保留用户偏好。
- RecentEntry：入口 id、项目 id、上次成功请求打开的时间；不等同于全系统最近文件。
- WindowAssociation：窗口句柄、目录证据、项目 id、识别状态；仅作为运行期信息，不当成长期稳定标识。

启动目标与参数分开存储，不将文件路径拼接成任意 shell 命令。成功提交启动请求与目标软件完全就绪是不同状态，界面反馈需准确。

路径检查、网络盘访问、图标提取均异步并设置超时或取消机制。默认普通用户权限，配置导入不自动执行任何入口。

## 9. 开发阶段与交付物

| 阶段 | 交付物 | 完成条件 |
|---|---|---|
| M0 原生能力验证 | 边缘唤出、原生材质和长按捕获小样；窗口识别实验记录 | 确认焦点、材质成本、全屏及多屏方案；明确资源管理器支持边界 |
| M1 视觉与交互定稿 | 水晶堆叠、毛玻璃浮层、滑选、网格、设置及异常状态设计 | 典型使用流程可以演示，动效和命中区域有具体参数 |
| M2 首版实现 | 启动入口、项目组织、搜索及设置 | 完成 P0 主流程，真实软件和文件夹可以打开 |
| M3 稳定性与分发 | 可安装或便携包、备份与恢复说明 | 验收清单通过，记录实际资源占用和系统兼容范围 |
| M4 项目窗口增强 | 窗口列表、切换、一键打开工作组合 | 只对已验证的窗口类型与标签能力做支持承诺 |

时间排期在 M0 明确系统兼容范围及窗口识别难度后制定，不先给出缺乏依据的工期承诺。

## 10. 验收清单

- [ ] 当前应用最大化时，可直接唤出并启动入口，无需回到桌面。
- [ ] 鼠标只是经过顶边时不频繁触发；操作浏览器标签和系统贴靠时可规避冲突。
- [ ] 连续进出热区 50 次，无卡死、闪烁或展开状态失控。
- [ ] 鼠标从快捷栏移动到项目面板、子菜单不会导致意外收起。
- [ ] 短按只打开主目录；长按不会同时启动主目录；一次选择只启动一次。
- [ ] 长按滑选、原地松手再点选、移出取消、Esc、失去捕获和排序模式切换无残余点击。
- [ ] 0、1、6、7、30 个其他目录时，布局、计数、命中及长名称显示符合定义。
- [ ] 背后是白色网页、深色应用及视频时，磨砂底板文字均可读；节能、关闭透明度模式仍保持完整层次。
- [ ] 默认是局部宽度圆角浮岛，增加项目不会铺满屏幕；顶边留白区可连续移动，不出现唤出后无法进入面板的断层。
- [ ] 宽度 320/640/1120、高度 64/88/160、图标 24/40/64 的组合均执行约束规则，超出屏幕尺寸时正确夹取。
- [ ] 系统文字缩放 100%/150%/200%，配合 DPI 缩放时不重复缩放；长名称、同名目录、中文与英文都可辨识。
- [ ] 入口过多时进入“更多”，不会重叠、无限缩小或改变固定顺序。
- [ ] 外观预览、取消、恢复默认与重启后恢复有效；拖滑块不持续写磁盘，滑选时不重排。
- [ ] 仅悬停唤出不抢键盘焦点，快捷键唤出可完整用键盘操作。
- [ ] 100%、125%、150%、200% 缩放下布局与热区正确；测试左右、上下双屏及显示器拔插。
- [ ] 全屏避让和快捷键主动唤出在目标系统上验证，记录例外情况。
- [ ] 含中文、空格的路径、快捷方式、默认文件关联和带参数应用启动正确。
- [ ] 失效路径、离线网络盘、权限不足有可操作的反馈，界面不冻结。
- [ ] 重启后项目、图标顺序及设置保留；损坏配置可恢复；导入不会触发启动动作。
- [ ] 搜索 500 个已添加入口时，结果展示目标为 100 ms 以内，注明测试设备及统计方式。
- [ ] 热区驻留计时结束到动画开始的目标为 50 ms 以内；展开动效目标约 200 ms。
- [ ] 在记录的测试设备上观察 10 分钟空闲状态：CPU 平均占用目标低于 1%，进程私有内存目标低于 150 MB；实际结果需测量，不是现有承诺。
- [ ] 记录标准/节能模式下应用 CPU、内存和 GPU，以及桌面合成器相对基线的 GPU 增量；收起后停止装饰动画，无持续高频绘制。
- [ ] 连续展开收起 200 次后，缓存和内存不持续增长；记录操作前、峰值和回落后的内存数据。
- [ ] P1 单独验收同名目录、父子项目、多窗口、多标签、外部打开窗口和无法关联的情况。

## 11. 后续需要确认的产品选择

这些问题不阻塞本计划；正式实现时优先确认：

1. 首发系统仅 Windows 11，还是也需要 Windows 10？默认 Windows 11。
2. 项目资料首版以手动固定入口为主，还是必须直接浏览目录内容？默认固定入口。
3. “切换已打开的项目窗口”是否是首发必须具备的能力？默认先验证，再按技术结果安排交付。
4. 是否经常使用多屏、全屏设计软件或游戏？默认支持选定屏幕和全屏避让。

已确认的交互选择：长按展开堆叠，滑向某个文件夹后松手打开。此项无需重复确认。

## 12. 参考依据

- 本地设计 skill：C:/Users/96311/.agents/skills/ui-ux-pro-max@claude/SKILL.md；已查询设计系统及 WPF 指南，按桌面场景筛选采用。
- [微软 WPF 概述](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/overview/)：桌面 UI、布局、样式与动画能力。
- [微软 AppUserModelIDs 文档](https://learn.microsoft.com/en-us/windows/win32/shell/appids)：任务栏关联及应用窗口分组机制，用于界定项目窗口功能边界。
- [微软 Windows 材质设计](https://learn.microsoft.com/en-us/windows/apps/design/signature-experiences/materials)：Acrylic、Mica 的用途区分。
- [微软 Acrylic 指南](https://learn.microsoft.com/en-us/windows/apps/design/style/acrylic)：毛玻璃材质及回退设计依据。

## 13. 当前交付边界

当前已有前端源码、可运行浏览器预览、生产 dist 构建、自动测试和内核交接文档。尚无原生可执行程序；屏幕热区、真实目录启动、系统背景磨砂、多屏与整体性能留给内核实施并验收。具体通过项与限制见 docs/验收与现状.md。
