# Luma 开发约束（所有代码代理先读）

2026-09-21 最新交付规则：用户明确要求“下次直接替换，不要搞什么候选版本”。今后完成修复和必要构建/回归后，直接更新 APP/native/Luma 与原便携 ZIP、安装 EXE，不再把单独候选版作为交付终点，也不重复询问替换许可。更新所需的当前 APP 实例退出、覆盖、重新启动已获授权，必须按完整 exe 路径隔离，保留用户配置、快捷方式及其他应用。不得在游戏/全屏期间抢前台；仅在实际更新时退出目标 APP，监测时仍只读。可保留内部构建暂存和回滚备份，但不要求用户选择或手动切换版本。长期稳定性/其他实机验收未完成要如实说明，不能因此把正常功能更新无限停留在候选阶段；已知会破坏数据或构建失败则先修复。这是后续交付偏好，不代表本次消息已执行替换。此条覆盖下文及历史报告、自动化中“现用 APP 运行就不得更新”“必须所有 GUI/五小时验收完成才可本地替换”等相冲突要求。

第十一轮补充：用户明确授权url收藏、软件recent、可选alias/fuzzy搜索以及明确分界的磁盘操作。最新docs/第十一轮交互与引用边界计划.md和协议第十一轮节。普通顶栏/集合拖拽、合并、X删除仅引用；实际目录新建/重命名/移动仅由有效目录令牌、明确操作表单和专用落点执行，禁覆盖/重解析替换/跨卷复制删除。磁盘提交后前端保留最终响应，不能8秒超时后丢弃结果。软件最近来自Windows有界样本非所有私有历史。2026-09-21已按用户指示覆盖0.3.1，交付记录以第十一轮报告最新覆盖交付节为准；WebView总内存50–150MB尚未达标，五小时长测仍未通过。自动启动识别支持明确test→Tauri dev→dev/start及手动覆盖，识别不执行不安装依赖。
第十轮补充：用户明确授权文件夹入口保存 launch:{command,workingDirectory}，仅根目录手动覆盖自动识别；执行仍经 project.detectTest/runTest 的保存ID和令牌，不能在调用参数中传命令/路径。新增 folder.getThumbnail 仅处理目录令牌对应文件，静态Shell缩略图、2工作线程与有界缓存，不播放预览。前端长按级联保留父层（最多12层），悬停250ms出现打开/进入大区域，持按450ms进入下层；布局移位不触发误打开，屏外列不扩张native命中区域。最终实现与实测见 docs/第十轮实现与验收报告.md。旧冻结挂机6503秒因DWM保护停止，未通过5小时，不能当作新候选稳定证据。

第九轮补充：收回中系统全局按键不得 CancelClose，原生前端 pointermove 不自行恢复 visible；真实鼠标返回由原生判定，保留650/600ms曲线。项目测试仅由当前目录令牌识别，新增 project.detectTest/project.runTest，不能接受任意路径或命令；识别不执行，点击后运行明确的已有测试入口，清单变更需重新识别。有界后台IO、超时不迟到启动、同目录终端防重复、禁自动依赖恢复。说明与未完成实测见 docs/第九轮交互与项目测试报告.md。不得将第八轮冻结挂机结果当作第九轮已通过真实交付的证据。

第八轮补充：用户要求纯图形无字母 Logo，最终统一绿色系叠片。preferences.width 作为初始宽度，置顶一级入口数量增加时扩展，1120/屏幕可用宽度封顶；子入口不撑大面板，横向过渡不逐帧同步原生区域。设置窗口最小化挂机约5小时曾导致系统卡顿，必须以最小化设置+收起浮岛为主场景长测，不能以短测代替。WebView Suspend/Resume 读取真实 IsSuspended，导航会自动恢复；初始布局确认前不能暂停浮岛。长期测试只用唯一exe副本和独立配置，退出按exe路径隔离，DWM不可读取记null。当前0.3.0是候选测试版，真实长测/全套交付验收未通过前不得发布成正式稳定版。详情 docs/第八轮阶段记录.md。

第七轮补充：用户确认“较高权限”仅指浮岛高于普通窗口，不申请管理员权限。v0.2.0 提供便携ZIP/当前用户安装EXE；系统入口方法见协议文末。Run值LumaQuickLaunch只由显式操作修改，默认关闭；桌面无关同名lnk不得覆盖，卸载保留用户配置和其他副本启动项。--startup不激活已有面板；--shutdown按完整exe路径隔离，禁止跨副本关闭。发布执行标准native、system integration、lifecycle与真实安装/升级/卸载检查。

第六轮补充：已有目标窗口按完整目录路径/安全软件身份复用，最小化恢复并前置，不永久置顶，不用标题猜测。三个启动出口共用有界后台服务；找到但Windows拒绝前置也不得再启动副本。带参数快捷方式、文档等保持原Shell语义，边界见 docs/第六轮修复报告.md 与协议文末。设置/搜索原生标题栏与WebView同步已保存主题，外观页统一内边距；不得把主题背景应用到透明浮岛。

第五轮补充：最新证据见 docs/第五轮修复报告.md。推出650ms、收回600ms，保持opacity=1与完整纵向位移；原生退出兜底1400ms。不要恢复短时淡出或弹跳。搜索/编辑/设置固定右侧，二级菜单88×94固定格子；打开当前目录参与长按滑选松手，移出取消。shell.getIcon只接收已保存入口ID，后台有界提取原软件PNG，不阻塞window.sync；保留品牌图像并加玻璃底座。协议文末覆盖历史动效参数。

第四轮补充：单目录长按必须读取真实一级目录；目录打开只传宿主令牌。动效使用 useDockMotion 的完整位移和固定扫过区域，禁止恢复逐帧 window.sync / SetWindowRgn。整理模式拖叠只改变快捷引用。最新规范见 docs/第四轮目录与成组修复计划.md 和 docs/内核接口协议.md 文末。

第三轮交互基线：浮岛退出保留 DOM 至动画完成，原生收到当前 visibilityId 的 collapsed 才最终隐藏；所有 window.visibility 事件递增编号，window.sync 回传，旧编号必须在更新命中区域/交互/状态前被忽略。不得恢复“即时 Hide”“按矩形数量猜测交互”“只支持 WM_MOUSEMOVE 的拖放热区”。设置拖放仅引用真实文件对象并保存，不移动文件；最新证据见 docs/第三轮交互修复报告.md。

## 本轮已确定的分工

- Codex：React / TypeScript 前端、视觉、项目管理交互、长按滑选、布局算法、桥接客户端和前端测试。
- Z Code / GLM 5.3：Windows 原生内核。直接执行 `docs/GLM-5.3-任务书.md`，按 `docs/内核接口协议.md` 实现。
- 用户已经选择局部顶边面板、圆角强磨砂、水晶图标、项目文件夹堆叠、长按滑选松手打开、宽高及图标可调。不要重新询问这些决定。
- 2026-09-19 用户明确指示：**管理工作台改为"驾驶舱"风格**（参照 MOMO 网关驾驶舱：顶部黑色细通栏、左侧白底分组边栏、米灰点阵纸感内容区、白底黑描边卡片、黑色大标题与大按钮、绿色状态点、大数字面板；信息架构=总览/项目/外观/系统四页）。由 Z Code 已实施；`src/contracts.ts` 与 `docs/内核接口协议.md` 未改动，浮岛与长按交互不变。"不改前端"约束自此由本条覆盖。

## 权威顺序

1. 用户最新明确指示。
2. `src/contracts.ts` 的类型与运行时数据校验；`docs/内核接口协议.md` 的消息语义。
3. 本文件和 `docs/GLM-5.3-任务书.md` 的分工与执行顺序。
4. `docs/前端设计规范.md`、`docs/验收与现状.md`。
5. 根目录《项目计划.md》《开源项目调研.md》是演进背景，已被本次前端架构决策覆盖的部分不得拿来重写当前代码。

## 约束

- 2026-09-19 原生修复以 `docs/原生审查修复报告.md` 为最新证据：禁止在全宽承载 HWND 上启用 DWM system backdrop；CSS 坐标按 WebView 实际 devicePixelRatio（含系统文字缩放）转换。原生改动发布后运行 `npm run test:native`，不能用模拟桥接替代。

- 内核代码放 `native/`，内核测试放 `native/tests/`。不要新建第二套前端、改 UI 框架、引入 Electron、替换 Windows Shell 或移动现有文件。
- 不改 `src/components/`、`src/styles.css`、图标、中文文案、长按规则和布局逻辑，除非用户明确要求，或有可复现的集成故障且以最小修改解决。
- 不改变协议方法名、JSON 字段名和 protocol=1。协议不满足需求时先记录具体复现场景与最小补充，不自行另建协议。
- 使用 Windows Shell 启动已保存且经验证的入口，不执行任意 shell 命令；前端只传 projectId/itemId。
- 原生模式不得回退到 demo。没有数据时返回空项目数组，不注入示例路径。
- 保存配置以 expectedRevision 乐观并发控制；原子写入与备份；保存不触发启动。
- 常驻时没有装饰动画、全盘递归扫描或每帧轮询。测量 WebView2 子进程与 DWM 增量，不只测宿主。
- 不安装或运行调研里的桌面替代软件。它们只是参考。
- 少问重复问题，先按任务书的默认值实现。错误先复现、定位、修复、再验证；不要把排查命令转交用户执行。
- 所有完成结论必须有最新测试输出；不要把浏览器模拟、宿主模拟器或截图当作真实 Windows 集成成功。

## 常用命令

`npm ci`、`npm run dev`、`npm run build`、`npm test`、`npm run test:e2e`。

前端 E2E 使用本机 Edge；测试设置保存在独立浏览器上下文，不影响用户配置。预览服务器只绑定 127.0.0.1。

- 2026-09-19 第二轮用户验收修复：新增协议方法与语义见 `docs/内核接口协议.md` 文末；当前证据见 `docs/第二轮验收修复报告.md`。原生 SearchWindow 必须导航明确的 `/index.html`，普通 WebMessage AdditionalObjects 可以为 null；浮岛初始隐藏，宿主 visibility 事件总是触发布局确认。搜索只查 Windows 索引，不擅自扩大索引范围。不要恢复旧的六方法计数、主屏限制或重复菜单。
